**Immediate Deployment Prompt for Claude/Cursor:**

Implement Project KAPPA tonight using attached full spec. Single laptop, Costa Rica field setup:

**Core Stack (Install Now):**
- **LTESniffer** (SysSec-KAIST): Passive decode of your 7 local LTE towers (712-01 Claro, 712-03 Liberty). Point RTL-SDR at Irazú volcano line-of-sight. Output JSON: `{imsi, tmsi, tower_id, timestamp}` to MQTT.
- **Sniffle** (nccgroup): Flash nRF52840 USB dongle ($10). Capture BLE advertisements (Android Auto UUID `0x2D00`, L3Mon beacons). Stream MACs to same MQTT broker.
- **kyanos** + **ptcpdump**: eBPF WiFi flow logging with process attribution. Detect 46.875 Hz timing patterns in packet inter-arrival (FFT on IAT).
- **KiwiSDR Remote**: Poll `kiwisdr.radioclub.cr:8073` waterfall API every 46.875s (the κ-second). Grab 15MHz spectrum when `gpredict` shows BLACKJACK/SDA satellites >30° elevation over Guácima (9.95°N, 84.15°W).

**The Correlation Rule (Implement First):**
```
IF (BLE.MAC appears) 
   AND (WiFi probe same MAC within 10s) 
   AND (LTE paging to that IMSI range within 30s)
   AND (Satellite elevation >30°)
THEN alert("Surveillance Handoff Detected")
```

**Tonight's Milestones:**
1. **Hour 1**: LTESniffer running on 1 tower, capturing IMSIs.
2. **Hour 2**: Sniffle dongle flashing, BLE MACs flowing.
3. **Hour 3**: kyanos WiFi → MQTT bridge.
4. **Hour 4**: Correlation engine (Python/redis) flagging MAC matches across BLE/WiFi/LTE.
5. **Hour 5**: Satellite pass trigger (gpredict TLE → KiwiSDR recording).

**Critical Config:**
- MQTT topic structure: `kappa/{domain}/{timestamp}` (local Mosquitto).
- 46.875 Hz detection: FFT 1024-point on WiFi packet timestamps (48kHz sample equivalent).
- MAC correlation window: 10 seconds (sliding).
- No RF transmission (passive only). Route MQTT through Tor for zero-signature.

**Deliverable by Dawn:** Single Python script `kappa_correlator.py` consuming MQTT streams from all 4 domains, printing JSON alerts to stdout when cross-domain temporal alignment detected. Include basic CLI dashboard showing tower counts and satellite passes.

Reference attached full KAPPA v2.0 spec for tool URLs and mathematical constants (κ=4/π, 46.875 Hz clock).

Execute.