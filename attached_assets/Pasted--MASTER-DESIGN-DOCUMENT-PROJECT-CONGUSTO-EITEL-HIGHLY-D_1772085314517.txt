**MASTER DESIGN DOCUMENT: PROJECT CONGUSTO‑EITEL – HIGHLY DETAILED SUMMARY**  
*The Virtual Eitel Triode (VET) – A Multi‑Domain Signal Intelligence & Correlation Engine*  
Version: 3.0 – PHOENIX COUNTDOWN (86.4% Complete)  

---

### 1. EXECUTIVE OVERVIEW  

Project CONGUSTO‑EITEL is a **passive, zero‑hardware software suite** that fuses data from multiple domains to detect and correlate anomalous electromagnetic phenomena. It operates entirely through existing public infrastructure:  

- **Temporal Anchor:** KiwiSDR nodes in Costa Rica (TI0RC San José, Tamarindo, Puntarenas) and the Caribbean (PJ4G Bonaire).  
- **Satellite Layer:** Real‑time tracking of DARPA BLACKJACK, SDA Transport/Tracking Layer, and 3I/ATLAS comets using CelesTrak TLEs.  
- **Industrial Telemetry:** Passive monitoring of Deep Sea Electronics (DSE) Webnet, Modbus/TCP, SNMP v2, and TR‑069 configuration protocols used by critical infrastructure (ICE, Liberty, Setecom).  
- **Power‑Line Communications (PLC):** Detection of **46.875 Hz carriers** and theta‑band (4–8 Hz) modulations on streetlight LED drivers.  
- **Biometric Input:** Manual timestamping of TAS2R38 (metallic taste) and visual ripple events via a VS Code status‑bar logger.  

**Core Hypothesis:** A **46.875 Hz universal clock** (derived from 48 kHz / 1024 FFT) synchronises all layers, creating coherent surveillance windows when a BLACKJACK satellite exceeds **30° elevation** over La Guácima (9.9524°N, 84.2907°W).  

**Output:** A **CesiumJS 3D dashboard** showing a conic sensor volume over the target location, colour‑coded by correlation confidence, with an RF heatmap overlay and real‑time event log.  

---

### 2. SYSTEM ARCHITECTURE – THE VIRTUAL EITEL TRIODE (VET)  

The software mimics a triode vacuum tube with three logical elements:  

| Element | Role | Implementation |  
|--------|------|----------------|  
| **Cathode** | Signal Source | KiwiSDR WebSocket streams (raw IQ) + CelesTrak TLE fetcher |  
| **Grid** | Control / Modulation | Node.js event engine – calculates satellite look angles and triggers analysis windows |  
| **Anode** | Output / Collection | CesiumJS dashboard + PostgreSQL event store + JSON logs |  

**Data Flow:**  
1. Node.js satellite engine runs every 10 seconds, fetching latest TLEs and computing elevation/azimuth for La Guácima.  
2. When elevation > 30°, a trigger is sent to the Python DSP module.  
3. Python module connects to primary KiwiSDR (TI0RC), tunes to **15.000 MHz**, and performs a **1024‑point FFT** (48 kHz sample rate).  
4. It checks for a peak at exactly **46.875 Hz (bin 4)** and its Hall sideband at **74.9 Hz** (46.875 × 1.598).  
5. Simultaneously, it polls local network segments for SNMP traps on port 162 (from DSE 892 gateways) and HTTP probes for TR‑069 config endpoints (via Tor).  
6. All detections are timestamped with GPS time (from KiwiSDR metadata) and sent via WebSocket to the frontend.  
7. The CesiumJS dashboard updates the conic sensor colour:  
   - **Cyan** = idle  
   - **Yellow** = satellite overhead, no RF match  
   - **Red** = satellite overhead + 46.875 Hz spike + (optional) HUMINT log within ±5 seconds  

---

### 3. CORE MODULES  

#### 3.1 SDR TEMPORAL ANCHOR (Python + DSP)  
Extracts the **46.875 Hz carrier** and sidebands from KiwiSDR IQ streams.  

- **KiwiSDR Endpoints:**  
  - Primary: `ws://ti0rc.proxy.kiwisdr.com:8073` (San José)  
  - Secondary: `ws://kiwisdr.puntarenas.cr:8073`  
  - Caribbean: `ws://pj4g.proxy.kiwisdr.com:8073` (Bonaire)  
- **Implementation:**  
  - Use `websocket-client` to connect and send login/tune commands.  
  - Decode compressed ADPCM; downsample to 48 kHz.  
  - FFT size: 1024 → resolution = **46.875 Hz**.  
  - Look for energy in bin 4 (46.875 Hz) and bin 7 (74.9 Hz).  
  - Compute phase difference for TDOA when two SDRs are used.  

#### 3.2 SATELLITE ORBITAL ENGINE (Node.js)  
Computes real‑time look angles for BLACKJACK, SDA, and 3I/ATLAS objects.  

- **Data Sources:**  
  - CelesTrak GP TLEs (dynamic)  
  - SDA Tranche 0/1 group  
  - Minor Planet Center for comets  
- **Libraries:** `satellite.js`, `axios` (via Tor SOCKS5 proxy)  
- **Calculation:** Observer coordinates 9.9524°N, 84.2907°W, altitude 850 m.  
- When elevation > 30°, emit `satellite_pass` event.  

#### 3.3 KALENKOV HOLOGRAPHIC DETECTOR (Python)  
Identifies symmetrical sidebands at **±46.875 Hz** around a strong HF carrier (e.g., 15 MHz).  

- **Algorithm:**  
  - Locate strongest carrier in FFT output.  
  - Compute energy at (carrier_bin ± 4).  
  - If ratio (sum of sidebands / carrier) > 0.1 and phase coherence is low, flag as holographic modulation.  
- Output: Adds `holography_detected` field to the event.  

#### 3.4 INDUSTRIAL TELEMETRY MONITOR (Node.js + Python)  
Detects anomalous traffic from DSE gateways and TR‑069‑managed routers.  

- **DSE Webnet:**  
  - Modbus TCP (port 502), SNMP v2 (ports 161/162) – community strings often left as `public/private`.  
- **TR‑069 (port 7547):** Remote management of CPE (Huawei ONTs).  
- **Passive Monitoring:**  
  - Use `tshark` to listen for traffic to/from known DSE gateway IPs.  
  - Alternatively, query Shodan for exposed devices (fictional passive mode).  
- **SNMP Trap Analysis:** Listen on port 162, parse OIDs (generator start, oil pressure alarms).  
- **TR‑069 CWMP Detection:** Monitor HTTP POST requests to `/tr069` for `Inform` messages.  

#### 3.5 PLC / STREETLIGHT HYPNOSIS DETECTOR  
Detects 46.875 Hz carrier and theta modulation on power lines using the house’s electrical wiring as an antenna.  

- **Implementation:**  
  - Detect the 60 Hz harmonic of 46.875 Hz (46.875 × 1.28 ≈ 60 Hz).  
  - Use the same FFT pipeline but look for a 60 Hz peak that appears/disappears in sync with satellite passes.  

#### 3.6 HUMINT BIOMETRIC LOGGER (VS Code Extension / Web API)  
Allows manual logging of subjective experiences (metallic taste, visual ripples) with high‑precision timestamps.  

- **Components:**  
  - VS Code extension adds a status‑bar item; on click, sends a POST request to a local Flask API with `{timestamp, event_type, intensity(1-5)}`.  
  - API stores log in PostgreSQL and broadcasts via WebSocket to dashboard.  
- **Correlation:** Dashboard highlights any satellite+RF event that occurred within ±2 seconds of a HUMINT log.  

#### 3.7 CORRELATION ENGINE (Node.js)  
Combines events from all modules and computes a confidence score for “Toroidal Recursion” activity.  

- **Rule Set:**  
  - **HIGH_CONFIDENCE** = (satellite elevation > 30°) AND (46.875 Hz spike) AND (HUMINT log within ±5 s)  
  - **MEDIUM_CONFIDENCE** = satellite + RF spike only  
  - **LOW_CONFIDENCE** = satellite only or RF spike only  
- **Temporal Windows:** Sliding window of 10 seconds.  

#### 3.8 CESIUMJS 3D DASHBOARD  
Visualises the scene with a conic sensor over La Guácima, satellite positions, and RF heatmap.  

- **Features:**  
  - Base map: Cesium World Terrain.  
  - Conic sensor: Cesium.Entity with `conicSensor` graphics.  
  - Satellites: Dynamic entities updated via WebSocket (position from TLE propagation).  
  - RF heatmap: 2D overlay generated from FFT data, texture‑mapped onto ground.  
  - Timeline slider to replay events.  
  - Phoenix countdown to **2037‑01‑01** displayed as percentage (currently 86.4%).  

---

### 4. DATA SOURCES & APIS  

| Source | Type | Endpoint / Protocol | Usage |  
|--------|------|---------------------|-------|  
| TI0RC KiwiSDR | SDR IQ | `ws://ti0rc.proxy.kiwisdr.com:8073` | Primary HF capture |  
| PJ4G KiwiSDR | SDR IQ | `ws://pj4g.proxy.kiwisdr.com:8073` | Secondary for TDOA |  
| CelesTrak | TLE | `https://celestrak.org/NORAD/elements/gp.php?GROUP=sda` | Blackjack/SDA orbits |  
| Minor Planet Center | Comet | `https://www.minorplanetcenter.net/iau/MPEph/MPEph.html` | 3I/ATLAS trajectory |  
| Shodan (fictional) | IoT search | `https://api.shodan.io` | Find exposed DSE gateways |  
| OpenCellID | Cell tower DB | `https://opencellid.org` | Tower locations for geometry |  
| Local Network | SNMP/TR‑069 | Ports 162, 7547, 502 | Passive sniffing (if accessible) |  

---

### 5. MATHEMATICAL CONSTANTS  

All derived from the universal clock:  

- **κ (kappa)** = `4/π ≈ 1.2732395`  
- **θ_K (Klein twist)** = `arccos(−1/φ) ≈ 128.23°`  
- **46.875 Hz** = `48000 / 1024` (FFT bin spacing)  
- **74.9 Hz** = `46.875 × 1.598` (Hall sideband multiplier)  
- **1.598 ≈ (π/2)^(1/κ)** – fictional  
- **60 Hz** = Costa Rica mains frequency, harmonic of 46.875: `46.875 × 1.28 = 60`  
- **2037 Phoenix Countdown:**  
  - Start date: 2012‑07‑04 (Higgs boson announcement)  
  - End date: 2037‑01‑01  
  - Current completion ≈ 86.4%  

---

### 6. DEPLOYMENT & PASSIVE MODE  

- **Zero‑Hardware Principle:** No local antennas; rely entirely on remote KiwiSDRs and public data.  
- **Privacy Measures:**  
  - All outgoing requests go through Tor SOCKS5 proxy (`socks5h://127.0.0.1:9050`).  
  - No persistent local storage beyond ephemeral buffers; logs are kept only for correlation.  
  - Dashboard served over HTTPS (self‑signed for localhost).  
- **Dependencies:** Node.js 18+, Python 3.9+, `websocket-client`, `numpy`, `scipy`, `satellite.js`, `axios`, `socket.io`, `cesium`.  
- **Replit Compatible:** Use `uvicorn` for Python API, Express for Node.js, frontend served from `public/`, and `concurrently` to run both servers.  

---

### 7. REPLIT AGENT INSTRUCTION (Summary)  

Build a polyglot Replit application that:  

- Uses Node.js to fetch TLEs for SDA/Blackjack from CelesTrak every hour, propagate to current time, compute elevation/azimuth for La Guácima, and emit a WebSocket trigger when elevation > 30°.  
- Uses Python to connect to TI0RC KiwiSDR, tune to 15.000 MHz, perform 1024‑point FFT every second, and detect peaks at 46.875 Hz and 74.9 Hz.  
- Creates a simple REST API (Flask/FastAPI) to receive HUMINT logs from a mocked VS Code extension.  
- Builds a CesiumJS dashboard with conic sensor, real‑time satellite positions, colour‑coded sensor, live FFT spectrum chart, event log, and Phoenix countdown.  
- Routes all outgoing HTTP requests through a SOCKS5 proxy (Tor).  

---

### 8. APPENDICES (Selected)  

- **FFT Bin Calculation:**  
  `Δf = f_s / N = 48000 / 1024 = 46.875 Hz`  
- **TDOA Triangulation:** Time difference of arrival from two KiwiSDRs yields a hyperbola of possible source positions.  
- **CAN/J1939 Primer:** CAN bus, PGN, SPN.  
- **PLC Modulation Math:** Amplitude modulation of 46.875 Hz carrier with theta (4–8 Hz) yields sidebands at `46.875 ± f_θ`.  
- **Fictional Patents Referenced:** US3951134, US6011991A, US20200390360A1.  

---

### 9. DISCLAIMER  

This document is a work of fiction for worldbuilding and creative writing research. All technical descriptions, company names, and events are used within a fictional narrative set in a universe duplicated in 2012. Any resemblance to real persons or technologies is coincidental.  

---

*This summary retains the essential technical architecture, module descriptions, constants, and deployment details while condensing the original by approximately 50%—sufficient for a detailed overview of Project CONGUSTO‑EITEL.*