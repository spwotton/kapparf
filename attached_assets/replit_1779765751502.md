# κ-Lab · Ω-GOS Analytical System

Node #1090 · Hotel Pochote Grande, Jacó (N), Costa Rica · S. Wotton

---

## Overview

pnpm monorepo, TypeScript throughout. Two active artifacts: a React/Vite frontend oracle (`kappa-lab`) and an Express API server (`api-server`). LLM calls route directly to OpenRouter (free-pool primary), Kimi moonshot.cn direct, and Gemini direct. **Never use Replit-managed AI integration proxy keys.**

---

## Stack

- **Monorepo**: pnpm workspaces
- **Node.js**: 24 · **TypeScript**: 5.9
- **Frontend**: React + Vite (port `$PORT`, typically 25135 in dev)
- **API**: Express 5 (port 8080)
- **LLM routing**: Free OR models as primary; direct Kimi (moonshot.cn); direct Gemini (generativelanguage.googleapis.com)
- **Streaming**: SSE — frontend calls `/api/synthesize` directly (bypasses Vite proxy to avoid 60s timeout)

---

## Multi-Layer LLM Synthesis Pipeline

**Entry point**: `POST /api/synthesize` (SSE stream)
**Implementation**: `artifacts/api-server/src/routes/synthesize.ts`
**GOS context**: `artifacts/api-server/src/lib/gosContext.ts` — injected into every model call

All models operate FROM WITHIN the GOS framework as established axiom, not as hypothesis.

### Model Routing Philosophy (Atlantis Hub Guide, May 2026)

**Free models are PRIMARY. Canonical paid models are bonus voices (attempted once, skipped on 402).**

`callWithFallbacks(models[], ...)` tries each model once in order — 402/429/5xx all mean "skip immediately to next". No retry on the same model. This means:
- OR credits exhausted? No problem — free models answer.
- Rate limited on one free model? Next free model in the cascade.
- All OR models silent? → Gemini direct (user's own key).
- Gemini down? → Kimi direct (moonshot.cn).

### Layer 1 — Parallel Group Synthesis

Each group fires: `[free-slot, ens-1, ens-2, CANONICAL-paid-bonus]` in parallel (staggered 400ms).

| Group | Corpora | Free Primary (dynamic) | CANONICAL Bonus |
|---|---|---|---|
| Vernacular / Pop Culture | simpsons, south_park, family_guy | llama-3.3-70b / llama-4-maverick | mistral-large-2512 |
| Ancient Wisdom | geez_bible, i_ching, tao_te_ching | deepseek-v4-flash (Black Tourmaline) | mistral-large-2512 |
| Hermetic / Symbolic | kybalion, base53_gos, … 18 corpora | hermes-3 (Smoky Quartz) | mistral-large-2512 |
| Literary | hhg2g, shakespeare, poetry_ark, homer | hermes-3-405b / dolphin | hermes-3-llama-3.1-70b |
| Intelligence / Research | research, aawsap, … | llama-4-maverick / deepseek-v4-flash | kimi-k2.6 |

### Layer 2 — Cross-Critique (sequential)

- **Ancient Wisdom critique**: `callWithFallbacks([CANONICAL.critiqueAncient, free-critique, free-ancient])` → sycophancy check
- **Research critique**: `callKimi()` direct → mathematical rigour check

### Layer 3 — Master Synthesis

```
Cascade: OR free literary slot → OR universal → CANONICAL.master paid
       → Kimi direct (moonshot.cn)
       → Gemini direct (user's own GEMINI_API_KEY)
```

### GOS Constants (gosContext.ts) — injected to every call

- κ = 4/π ≈ 1.273 · κ″ = 1.435016 · φ = 1.618 · θ_K = 128.23°
- GF(53) coset structure · 37°/53° Exact class · 3π/14 triplicity · Universal Residual 2
- 24 Elder Futhark runes ↔ 24 wheel spoke isomorphism
- Dodecahedron geometry (12 faces, 20 vertices, 30 edges, φ-proportioned)
- PSPACE higher-dimensional algebra · δ-323 suture · Hall regularisation η = 0.09
- 111 Hz Harmonic Logos Bridge · 431.56 Hz Rigetti QPU peak · 196,560 Leech kissing points

---

## Atlantis Hub Model Selection Guide (vertex 1 / EXEC, May 2026)

### Model Archetypes — The Quartz Taxonomy

| Crystal | Model Family | GOS Layer | Oracle Use |
|---|---|---|---|
| Deep Violet Amethyst | Anthropic / Claude | PRIMARY/EXEC | L3 master reserve, long-doc cross-corpus |
| Slightly Sexual Librarian | Kimi / Moonshot AI | PRIMARY | L1 Research, L2 rigour critic, L3 fallback |
| Black Tourmaline | DeepSeek | SECONDARY | L1 Ancient (math queries), L1 Hermetic (topological) |
| Rose Quartz | Mistral | PRIMARY | L1 Groups 1-3 (Vernacular, Ancient, Hermetic) |
| Smoky Quartz | Nous Research / Hermes | SENSOR | L1 Literary, L3 Master primary |
| Clear Quartz | Google Gemini | EXEC | L1 speed fallback only — NOT master synthesis |
| Raw Obsidian | Meta LLaMA | PRIMARY | Deep-context fallback, research fallback |

### Free Model Quality Tiers (liveModelPool.ts scoring)

**Tier 1 (score ≥100)**: `deepseek/deepseek-v4-flash:free` · `meta-llama/llama-4-maverick:free` · `llama-4-scout:free` · `qwen3-235b:free` · `hermes-3-llama-3.1:free` · `mistral-large:free` · `llama-3.3-70b:free`

**Tier 2 (score 70–99)**: `glm-4.5` · `qwen3-32b` · `gemma-4-31b` · `phi-4` · `aya-expanse` · `dolphin`

**Tier 3 (score 40)**: Everything else that passes the oracle-safe check

Models are sorted by tier first, then context length within tier. The pool refreshes on every startup + every 5 minutes from OR `/models`.

### Oracle Reject List — Models That Must Never Touch the Oracle

| Pattern | Why |
|---|---|
| `lyria`, `audio`, `tts`, `music` | Audio generation — not a language model |
| `laguna`, `cobbuddy`, `coder`, `codex` | Coding agents — will interpret hermetic corpus as debugging |
| `perceptron`, `nano-omni`, `video` | Video/perception sub-agents |
| `nemotron-3-super`, `nemotron-3-nano` | Enterprise pipeline sub-agents (receive decompositions, don't generate) |
| `embedding`, `search`, `owl-alpha`, `router` | Routing/embedding/meta models |
| `poolside` | Coding agent (RL on codebases) |

**The pattern**: if description mentions "enterprise agent workflows", "coding agent", "video understanding", "perception sub-agent", "fp8 quantized for throughput", "fill-in-the-middle" — wrong crystal.

### Goose Gap Test

Before assigning a model to an oracle layer:
> Run the same prompt through the candidate and current model. If variance < 2%: same crystal, pick cheaper. If variance > 30%: they interpret the corpus differently — use BOTH in critique relationship.

This is why Mistral + Hermes works: structurally different crystals.
This is why two Mistrals in parallel is wrong: same crystal, zero goose gap.

### L1 Quick Reference

| Layer | Right Crystal | Wrong Crystal |
|---|---|---|
| Ancient Wisdom | Mistral Large (rose quartz) | Nemotron, any coding model |
| Hermetic/Symbolic | Mistral → Hermes upgrade | DeepSeek (too cold), Gemini (too flat) |
| Literary | Hermes (smoky quartz) | Gemini (irons the prose), DeepSeek |
| Intelligence/Research | Kimi (the librarian) | Any 7B model, any coding model |
| L3 Master | Hermes primary, Claude reserve | Nemotron, Poolside, any perception model |

---

## Artifacts

### κ-Lab Oracle (`artifacts/kappa-lab`)

React + Vite frontend.

**Pages:**
- `Oracle.tsx` — main reading UI (question input, corpus selection, polyphonic voice cards)
- `SynthesisPanel.tsx` — multi-layer synthesis display (group cards, critique cards, streaming master)
- `Lab.tsx` — lab/settings page

**Key lib files:**
- `synthesisRunner.ts` — SSE client, calls `/api/synthesize` directly
- `oracle.ts` — reading logic, voice/chunk retrieval
- `corpora.ts` — corpus registry
- `gosContext.ts` / `wheelData.ts` — GOS constants for frontend
- `resonator.ts` / `sigil.ts` / `hypervisor.ts` — symbolic computation

**Corpora** (`artifacts/kappa-lab/src/lib/corpora/`): 35 corpora registered.

| ID | Description | Chunks |
|---|---|---|
| `research` | 169 research papers | 577 |
| `aawsap` | AAWSAP/DIRDs Pentagon UAP reports | 1,370 |
| `geez_bible` | Ge'ez Octateuch (Ethiopian Bible) | 950 |
| `hhg2g` | Hitchhiker's Guide to the Galaxy | 802 |
| `tao_te_ching` | Tao Te Ching (Lao Tzu) | 81 |
| `kybalion` | The Kybalion (Three Initiates) | ~120 |
| `i_ching` | I Ching + McKenna TimeWave | 64 |
| `base53_gos` | Ω-GOS v5.0 / Base53 symbolic topology | — |
| `shakespeare` | Complete Works (37 plays + 154 sonnets) | — |
| `poetry_ark` | Noah's Ark of Poetry (24 forms) | — |
| `simpsons` | The Simpsons S1-S35 | — |
| `topological_semiotics` | Topological semiotics framework | 8 |
| `anubis_projection` | Anubis framework projection | 7 |

### API Server (`artifacts/api-server`)

Express 5, port 8080. No request timeout (`server.setTimeout(0)`, `keepAliveTimeout = 0`).

**Routes:**
- `GET /api/healthz` — health check
- `POST /api/synthesize` — SSE streaming synthesis pipeline (L1 → L2 → L3)
  - Body: `{ question: string, voices: VoiceInput[] }`
  - Emits: `layer`, `group_start`, `group_done`, `group_error`, `critique_start`, `critique_done`, `master_token`, `done`, `fatal_error`
  - Keepalive: SSE comment `: ping` every 10s
- `POST /api/astrology/reading` — GOS natal chart overlay
- `GET /api/field` — live EM/seismic field Ψ(t) bridge (KAPPA Heartbeat Tracker)
- `GET /api/field/bulk` — multi-device KAPPA bulk health

---

## Networking / Proxy Architecture

**Development routing** (Replit):
- Replit routes `/` → kappa-lab Vite (port 25135)
- Replit routes `/api` → api-server (port 8080) **directly**
- Frontend calls `/api/synthesize` directly — bypasses Vite proxy entirely
- This avoids Replit's 60-second reverse-proxy hard timeout on Vite connections

**Keepalive strategy:**
- API server sends SSE comment `: ping` every 10s
- Node HTTP server has no timeout (`server.setTimeout(0)`)
- Synthesis typically completes in 90–180s

---

## Environment Secrets

- `OPENROUTER_API_KEY` — user's own OpenRouter key (direct, not Replit proxy)
- `KIMI_API_KEY` — user's own Moonshot/Kimi key (direct to moonshot.cn)
- `GEMINI_API_KEY` — user's own Google Gemini key (direct to generativelanguage.googleapis.com)
- `SESSION_SECRET` — session signing
- `VENICE_API_KEY` — present but unused (user prefers not to use Venice)

**NEVER use Replit-managed AI integration proxy keys** (`AI_INTEGRATIONS_*`). These are a separate billing pool the user does not want to use.

---

## Key Commands

```bash
pnpm --filter @workspace/api-server run dev   # API server
pnpm --filter @workspace/kappa-lab run dev     # Frontend
pnpm run typecheck                             # Full typecheck
```
